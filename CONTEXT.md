# Road Desk

高速公路路段场景下的远程桌面远控软件：让授权人员像使用 VNC 一样，远程查看并操作另一端 Windows 计算机的桌面，用于机电设备远程维护。

## Language

**远控**:
授权操作员通过网络实时查看并操作远端计算机桌面的能力（类 VNC）。仅面向 Windows。
_Avoid_: 设备控制, SCADA, 外场设备遥控, 远程配置下发, 跨平台桌面

**远端桌面**:
被远控的那台计算机所呈现的图形界面会话。
_Avoid_: 远程主机（除非特指机器本身）, 屏幕（除非仅指画面传输）

**操作员** (Operator):
发起远控的人，包括监控室值班人员、收费站及中心的机电维护人员。
_Avoid_: 用户, 客户端, 管理员（除非特指系统管理角色）, User（过泛）

**被控端** (Host):
安装远控服务、可被操作员接入并操作桌面的 Windows 计算机。典型包括车道电脑、机电工作站、Windows 服务器。英文文档与代码标识统一用 Host。
_Avoid_: 客户端, 节点（过泛）, 设备（易与机电外场设备混淆）, Controlled endpoint, Slave, Server（易与 Windows Server 操作系统或「中心服务器」混淆）

**车道电脑**:
安装在收费车道、运行车道业务软件的 Windows 工控/台式计算机。
_Avoid_: 车道工控机（除非现场确用此称呼且需单独区分）

**无人值守接入**:
被控端常驻远控服务，操作员凭授权即可建立远控，无需现场人员确认；不得向车道业务界面弹出确认框或打断收费操作。
_Avoid_: 远程协助, 需同意接入, 邀请式远控

**共享控制台**:
远控操作的是被控端当前本地正在显示的那个桌面会话，而非另开的 Windows 登录会话。
_Avoid_: 独立会话, 远程桌面会话（易与 RDP 多会话混淆）, 影子会话（除非特指旁观）

**控制模式**:
一次远控会话中操作员对被控端的介入程度。目标能力包括：仅查看、控制且锁定本地输入、控制且与本地共享输入。
_Avoid_: 权限（过泛，易与账号授权混淆）

**共享输入**:
远控控制模式下，本地键鼠与远程操作员可同时作用于同一桌面。MVP 仅支持此控制模式。
_Avoid_: 双控, 协同操作（除非明确做协作产品）

**本地输入锁定**:
远控控制期间禁用被控端本地键盘鼠标，断开或结束后恢复。目标能力，非 MVP。
_Avoid_: 锁屏（易与 Windows 锁定会话混淆）

**远控会话**:
一名操作员与一台被控端之间已建立的桌面查看/控制连接。允许多个 Viewer 同时连接同一 Host（共享控制台、均可注入键鼠，默认上限 8）；不做旁观/控权转移。
_Avoid_: 连接（过泛）, 通道

**旁观**:
目标能力：在已有控制者的远控会话上，额外操作员只读查看同一共享控制台，不注入输入。
_Avoid_: 影子（除非内部协议用语）, 监控（易与视频监控混淆）

**控制权**:
目标能力：在一控多看场景下，当前唯一可以向被控端注入键鼠输入的操作员身份；可在旁观者之间转移。
_Avoid_: 主机权, 操作权限（过泛）

**接入授权**:
决定某操作员能否对某被控端发起远控。MVP 为扁平授权：持有预共享口令即可连接网络可达的被控端；目标再叠加管理端账号，并可对接被控端目录中的对象。
_Avoid_: 权限系统（过泛）, ACL（除非已实现到机器级）

**审计日志**:
目标能力：记录远控连接事件与操作元数据（谁、何时、哪台被控端、时长、发起端、成功/失败/互斥拒绝、查看或控制、是否使用剪贴板/文件传输、断开原因等）。不含会话录像。一期话单已上（Viewer+Host 双源 upsert，管理端「审计」可查）。桌面行为 **A5a–A5d 已落地并实验室验收通过（2026-08-06）**：一路控制、进程开/关、前台窗焦点/标题、敏感字段折叠与导出。详见 [docs/audit-implementation-plan.md](docs/audit-implementation-plan.md) §4.3。
_Avoid_: 会话录像, 录屏审计, 操作回放, 键鼠轨迹

**操作员账号**:
操作员登录远控体系所用的身份凭据。MVP 仅提供唯一测试账号；目标由管理端维护多名操作员账号。
_Avoid_: Windows 登录账号（除非明确对接 AD）, 系统用户

**管理端**:
目标能力：供管理人员维护操作员账号及后续运维配置的管理界面或系统。
_Avoid_: 后台（过泛）, 控制台（易与被控端控制台桌面混淆）

**被控端目录**:
可供操作员检索并发起远控的被控端清单。一期：中心机 **Go 网关 + Docker Postgres** 维护站级分组树与标签；Host Agent 注册/心跳；Viewer 用操作员帐号登录或 `viewer.psk` 拉取真目录并直连媒体。未配网关或 `--demo-book` 时仍可用内置高速演示树。见 `docs/gateway.md`。
_Avoid_: 资产管理系统（除非明确做资产树）, 通讯录（可作别名但易偏人际）

**直连**:
操作端与被控端之间直接传输桌面画面与键鼠数据，不经中心转发。MVP 仅支持直连。
_Avoid_: P2P（除非确指打洞实现）

**中继**:
目标能力：桌面流量经中心节点转发，用于两端路由不可达时仍能远控。目标为能直连则直连，否则中继。
_Avoid_: 代理（易与被控端代理程序混淆）, 网关（过泛）

**已登录桌面远控**:
MVP 范围：仅在被控端已有用户登录并处于其桌面会话时可远控。
_Avoid_: 完整无人值守（易夸大当前能力）

**登录前远控**:
目标能力：在 Windows 登录界面或锁屏状态下仍可查看并注入输入（如输入凭据登录/解锁）。不含 UAC 安全桌面，除非另行立项。
_Avoid_: 安全桌面远控, UAC 远控

**剪贴板同步**:
目标必选能力：远控会话中操作端与被控端之间同步剪贴板内容。已实现：双向纯文本（`CF_UNICODETEXT`）、位图/截图（`CF_DIB`，zlib）、经剪贴板的多文件/目录（`CF_HDROP`，mux File 通道分块 + 本地 staging）。出站优先级 HDROP > DIB > TEXT。
_Avoid_: 云剪贴板

**文件传输**:
目标必选能力：在远控会话中于操作端与被控端之间传输文件。剪贴板文件/目录同步已覆盖「复制→粘贴」路径；独立传文件 UI 仍不做。
_Avoid_: 网盘, 共享文件夹（除非借用外部通道，不属于本产品文件传输）

**操作端** (Viewer):
操作员用来发起远控的 Windows 桌面客户端。不提供浏览器远控。需支持 Win7 x64（与监控室/维护现场老岗位机一致）。英文文档与代码标识统一用 Viewer；口语「控制端」若出现，等同 Viewer，书面避免。UI：Win32 管理台（左树演示目录 + 中间主机列表/会话工作区 + 底栏）；远控会话默认嵌在工作区 Tab，可拖成独立窗。CLI 仍可 `host:port` 直连单窗。Win7 线不用 Qt；未来 Win10 线可另开 Qt6。
_Avoid_: Web 客户端, 控制端, 主控端, Client（过泛）, Controller（易与控制模式/控制权混淆）, 仅 Win10/11 操作端

**被控端代理** (Host Agent):
安装在被控端（Host）上、提供无人值守共享控制台接入的 Windows 程序（服务/常驻进程）。英文文档与代码标识统一用 Host Agent；可简称 Agent，但仅在已明确是 Host 侧时使用。
_Avoid_: 客户端（与操作端/Viewer 混淆）, 控制端, Server 程序（易与 Windows Server 或中心服务混淆）

**传输加密**:
操作端与被控端之间的远控数据（画面、键鼠，以及后续剪贴板、文件传输）全程加密。MVP 即启用，不提供明文模式。
_Avoid_: 可选加密, 仅信令加密

**预共享口令**:
MVP 认证方式：被控端代理与操作端预先配置同一口令，连接时校验；界面可呈现固定测试用户名，但 MVP 不依赖中心账号服务。
_Avoid_: 中心登录（MVP）, 单点登录（MVP）

**静默代理**:
目标形态：被控端代理无托盘、无气泡、无水印、无业务弹窗，仅以后台服务/常驻进程运行。MVP 可用控制台程序输出调试信息，不属于目标形态。
_Avoid_: 远程协助提示, 会话水印（已明确不做）

**主屏远控**:
仅传输被控端主显示器画面。多显示器选屏或传全部显示器**暂不实现**（现场少见；未排期，勿当 Viewer/Host 近期待办）。
_Avoid_: 虚拟桌面（Windows 多桌面，与多屏不同）, 多屏选屏 UI, 跨显示器拼接桌面

**共享控制台远控（产品定位）**:
本产品补足 RDP 等独立会话远控不适合的场景——尤其车道电脑需操作当前可见桌面、且不能弹窗干扰收费。与 RDP/第三方远控可并行存在，不要求互斥或替换。
_Avoid_: 统一远程桌面平台, RDP 替代品

**媒体面**:
远控会话里传输「桌面内容与操作」的那一层：屏幕采集、编码压缩、画面发送、键鼠/剪贴板/文件等实时数据通道。类比打电话时的「语音流」。
_Avoid_: 多媒体业务, 媒体服务器（除非确指中继转发画面的节点）

**控制面**:
安排和约束远控会话的那一层：登录账号、被控端目录、能否接入、会话互斥/旁观与控制权、中继协调、审计日志。类比打电话时的「呼叫、鉴权、挂断、话单」。
_Avoid_: 被控端控制台桌面, Windows 控制面板

**媒体面适配器**:
MVP 策略：媒体面先以 VNC 系实现，并隔离在可替换适配器后；控制面始终自研。曾有两版完全自研媒体面失败，故不在 MVP 再次全量自研媒体面。前序探针总判「继续 A」；合规换芯路径见 ADR-0004 与 docs/spike-media-replace.md（自研 C++、GDI、TLS mux；不迁 Go/Rust）。
_Avoid_: 整包 Fork VNC 当产品, 一上来全量自研媒体面

**桌面远控（非视频监控）**:
本产品传输的是被控端桌面帧，用于操作 Windows GUI；不是路侧摄像机视频监控系统。历史自研中因兼顾桌面内视频区域观感导致帧率崩坏，MVP 不以「桌面内视频流畅」为成功标准。
_Avoid_: 视频监控, 录像回放平台, 把远控当视频流媒体做

**GUI 操作优先**:
远控验收以菜单、按钮、对话框、配置页等 GUI 可准确查看与操作为准。桌面内若有视频预览区，允许卡顿或画质下降，不作为 MVP 成功条件。
_Avoid_: 全屏高帧率, 视频区流畅优先

**MVP 验收**:
内网直连下：预共享口令登录、传输加密、共享控制台键鼠可完成一次业务 GUI 操作；点击反馈达内网可感知流畅（非对标本地）；连续远控不少于 30 分钟无黑屏/闪屏/异常掉线；允许多 Viewer 共享控制同一被控端。体验对齐经典 WinVNC（TightVNC 一档）的看屏与键鼠手感，不钉死某一 5.x build；不以桌面内视频区流畅为条件。
_Avoid_: 以桌面内视频区 FPS 为验收, 以功能清单替代稳定性出门标准, 对标某一不确切的 WinVNC 5 安装包功能全集

**媒体面许可策略**:
MVP 内部验证可临时使用 GPL 等 copyleft 的 VNC 组件；正式对外交付前必须换成合规底座（商业授权、宽松许可或自研适配器实现）。媒体面适配器边界服务于此替换。
_Avoid_: 未审许可就打进正式安装包, 假设开源等于可任意商用
