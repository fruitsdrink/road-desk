# Copy to .env.development.local and set the gateway first-boot admin password:
#   copy .env.development.example .env.development.local
# Password is in tools/gateway/data/admin-credentials.txt (or gateway startup log).
VITE_DEV_ADMIN_PASSWORD=
